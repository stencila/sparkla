language: node_js
node_js:
  - '10'

script:
  - npm run lint
  - npm run build

deploy:
  # Do a semantic release (only on Linux)
  - provider: script
    skip_cleanup: true
    script: npx semantic-release
    on:
      branch: master
      condition: $TRAVIS_OS_NAME = linux

